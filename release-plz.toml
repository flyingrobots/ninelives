release = false # workspace releases disabled; package-level opts control publishing
pr_labels = ["release", "release-ready"]

[workspace]
semver_check = true

[[packages]]
name = "ninelives"
release = true

# [P2.15.b] Define `AuditLayer` (logs all commands) (tests & docs)

### [P2.15.b] Define `AuditLayer` (logs all commands) (tests & docs)
- Steps:
  - Add tests
  - Document & wire up
- Estimate: ~1-2h
- Scope: In scope: Define `AuditLayer` (logs all commands). Out of scope: unrelated transports/services.
- Complexity: ~60 LoC


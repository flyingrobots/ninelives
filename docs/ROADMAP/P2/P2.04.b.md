# [P2.04.b] Define `CommandHandler` trait as `tower::Service<CommandContext>` (tests & docs)

### [P2.04.b] Define `CommandHandler` trait as `tower::Service<CommandContext>` (tests & docs)
- Steps:
  - Add tests
  - Document & wire up
- Estimate: ~1-2h
- Scope: In scope: Define `CommandHandler` trait as `tower::Service<CommandContext>`. Out of scope: unrelated transports/services.
- Complexity: ~60 LoC


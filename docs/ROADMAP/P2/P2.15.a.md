# [P2.15.a] Define `AuditLayer` (logs all commands) (core implementation)

### [P2.15.a] Define `AuditLayer` (logs all commands) (core implementation)
- Steps:
  - Plan Define `AuditLayer` (logs all commands)
  - Implement core
- Estimate: ~2h
- Scope: In scope: Define `AuditLayer` (logs all commands). Out of scope: unrelated transports/services.
- Complexity: ~60 LoC

